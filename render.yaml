services:
  - type: web
    name: advanced-smart-navigation-ai
    env: static
    buildCommand: npm install
    startCommand: npx serve -s . -p $PORT
    staticPublishPath: .
    envVars:
      - key: NODE_ENV
        value: production 